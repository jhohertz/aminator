enabled: true
# possible OS device prefixes
device_prefixes: [xvd, sd]
# think device_prefix + device letter (sda, sdb, xvda, etc)
# slice uses f-p, for now, just pick a few after that so there's no collide
device_letters: 'qrstuv'
